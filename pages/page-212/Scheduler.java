// 고객센터 전화 배분 스케줄러 인터페이스
// 다양한 배분 정책을 구현할 수 있도록 추상화
public interface Scheduler {
    // 대기열에서 다음 전화를 가져오는 메서드
    // 각 구현체마다 다른 우선순위 로직 적용 가능
    void getNextCall();
    
    // 가져온 전화를 상담원에게 배분하는 메서드
    // 각 구현체마다 다른 배분 정책 적용 (순서대로, 짧은 대기열, 우선순위)
    void sendCallToAgent();
}
